version: "3.9"
services:
  taskflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: taskflow:latest
    volumes: 
      - ../../..:/app/
    networks:
      - web-proxy

networks:
    web-proxy:
       external: true